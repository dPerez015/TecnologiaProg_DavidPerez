#pragma once

enum class ObjectID : const int {//evita comparaciones con otros enum y permite especificar el tamaño de los datos (:const int)
	SNAKE_HEAD, SNAKE_BODY, SNAKE_TAIL, SNAKE_CURVA,COMIDA
};
enum class FontID : const int {
	
};